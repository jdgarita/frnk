# Gradle
.gradle/
build/
**/build/
!gradle/wrapper/gradle-wrapper.jar

# IntelliJ / Android Studio
.idea/
.claude/
*.iml
*.iws
*.ipr
out/
.kotlin/

# Local config & secrets
local.properties
keystore.properties
*.keystore
*.jks

# OS
.DS_Store
Thumbs.db

# Xcode
xcuserdata/
*.xcuserstate
*.xcworkspace/xcuserdata/
DerivedData/
**/Pods/

# SwiftPM
.swiftpm/
.build/
# Xcode-managed SwiftPM pins for iosDemoApp — resolved locally on first Xcode open.
# Kept local (like Secrets.xcconfig below): the demo apps are internal smoke harnesses that
# already require local key setup, and CI never builds iOS.
**/xcshareddata/swiftpm/Package.resolved

# CocoaPods (if reintroduced later)
Podfile.lock
demo/ios-app/iosDemoApp.xcodeproj/xcuserdata/
# iOS demo keys (Sentry DSN / PostHog / RevenueCat) — same approach as Faint: Config.xcconfig does
# `#include? "Secrets.xcconfig"`; copy Configuration/Secrets.xcconfig.template and fill it in.
demo/ios-app/Configuration/Secrets.xcconfig
demo/ios-app/iosDemoApp.xcodeproj/project.xcworkspace/xcuserdata/

# Kotlin Multiplatform
*.kexe
*.kexe.dSYM/

# CI artifacts
reports/
test-results/
.hotswan/
