# Firebase Crashlytics CLI scratch dir (created by upload-symbols / firebase CLI)
.crashlytics/

# Gradle
.gradle/
build/
**/build/
!gradle/wrapper/gradle-wrapper.jar

# IntelliJ / Android Studio
.idea/
.claude/
*.iml
*.iws
*.ipr
out/
.kotlin/

# Local config & secrets
local.properties
keystore.properties
*.keystore
*.jks
google-services.json
GoogleService-Info.plist
fastlane/.env*

# OS
.DS_Store
Thumbs.db

# Xcode
xcuserdata/
*.xcuserstate
*.xcworkspace/xcuserdata/
DerivedData/
**/Pods/

# SwiftPM
.swiftpm/
.build/
# Xcode-managed SwiftPM pins for iosDemoApp — resolved locally on first Xcode open.
# Kept local (like GoogleService-Info.plist above): the demo apps are internal smoke
# harnesses that already require local Firebase setup, and CI never builds iOS.
**/xcshareddata/swiftpm/Package.resolved

# CocoaPods (if reintroduced later)
Podfile.lock
demo/ios-app/iosDemoApp.xcodeproj/xcuserdata/
demo/ios-app/iosDemoApp.xcodeproj/project.xcworkspace/xcuserdata/

# Kotlin Multiplatform
*.kexe
*.kexe.dSYM/

# CI artifacts
reports/
test-results/
